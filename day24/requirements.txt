# День 24 — цитаты, источники, анти-галлюцинации (поверх Дней 21–23).
# Ядро (grounded.py) переиспользует поиск Дня 22 и реранкер Дня 23; здесь добавлена
# только проверка цитат кодом (rapidfuzz) и контракт через json_object.
# Провайдер модели переключается ключом в .env: DeepSeek (основной) или OpenRouter (фолбэк).
sentence-transformers>=5.0      # эмбеддер bge-m3 (День 21) + cross-encoder реранкер (День 23)
torch>=2.0                      # backend для моделей
openai>=1.40                    # клиент к DeepSeek / OpenRouter (контракт + судья)
rapidfuzz>=3.0                  # fuzzy-проверка дословности цитат (узел 2)
sqlite-vec>=0.1                 # чтение индекса Дня 21
fastapi>=0.110                  # веб-демо (стеклянный ящик)
uvicorn>=0.29                   # ASGI-сервер
