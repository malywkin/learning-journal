# День 33 — секреты НЕ коммитим. Скопируйте в .env и впишите свой ключ.
# Основной провайдер — DeepSeek (OpenAI-совместимый). Запасной — локальный LM Studio
# (ключ не нужен). Если ключа DeepSeek нет, мотор сам падает на локальную модель (§14).
DEEPSEEK_API_KEY=
