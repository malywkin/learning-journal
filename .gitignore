# Секреты — НИКОГДА не коммитим (.env.example остаётся, это шаблон без ключа)
.env
*.key
credentials*

# Виртуальные окружения и кэш Python
.venv/
venv/
__pycache__/
*.pyc

# macOS / IDE мусор
.DS_Store
.idea/
.vscode/

# Видео — сдаём ссылкой в таблицу курса, в гит НЕ кладём (тяжёлые бинарники)
*.mp4
*.mov
*.mkv
*.webm

# Память агента / локальные данные диалога — приватная переписка, не коммитим
# (memory.py — это КОД, его оставляем; прячем только данные)
memory.json
memory.db
*.sqlite
*.sqlite3
mem_*.json
mem_*.txt
*.tmp

# День 13 — файл состояния задачи (локальные данные прогона, не код)
state.json
state_*.json

# День 14 — пользовательские самозапреты и тестовые прогоны (локальные данные, не код)
invariants_user.json
*_inv.json

# Временные пробники/раннеры (живут локально, в репозиторий не идут):
# _*.py — черновые скрипты (_run, _exp, _smoke), probe/ping — разовые зонды API
_*.py
probe.py
ping.py

# Доп. секреты (расширенный список, на всякий случай). .env.example остаётся.
.env.*
!.env.example
*.pem
*.p12
*.pfx
*.keystore
id_rsa*
*.token
