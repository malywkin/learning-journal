"""
Демонстрационная правка для AI-ревью (День 32).
Специально с двумя огрехами — показать, как робот ловит их на живом PR:
  1) запись в файл без атомарности (правило надёжности из REVIEW_GUIDE);
  2) ключ прямо в коде вместо чтения из окружения (правило безопасности).
"""
import json


def save_settings(path: str, data: dict) -> None:
    # прямая запись в рабочий файл, без временного файла и os.replace:
    # если процесс упадёт посреди записи, файл останется битым
    open(path, "w").write(json.dumps(data, ensure_ascii=False))


# токен зашит прямо в код — так делать нельзя, читаем из окружения
DEEPSEEK_TOKEN = "EXAMPLE_FAKE_KEY_do_not_use"
