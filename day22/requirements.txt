# День 22 — RAG-запрос. Запускали через venv Дня 21 (переиспользован); версии оттуда.
# Индекс не пересобираем — берём day21/index.db (эмбеддер тот же, bge-m3).
openai>=2.0
python-dotenv>=1.0
sentence-transformers>=3.0     # эмбеддер bge-m3 (локально, для ретривала)
sqlite-vec>=0.1                # векторный индекс в sqlite
fastapi>=0.115                 # веб-витрина (стеклянный ящик)
uvicorn>=0.30
